import React from 'react';
import Menu from '../Menu/Menu';
import NavBar from '../NavBar/NavBar';

const Home = () => {
    return (
        <div>
            <NavBar />
             
            <Menu />
        </div>
    );
};

export default Home;